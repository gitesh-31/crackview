import { LeaderboardEntry } from "@/types/database";

// Real leaderboard data will come from Supabase once friends are added
// For now, start empty — real users will populate this as they use the platform
export const mockLeaderboard: LeaderboardEntry[] = [];
